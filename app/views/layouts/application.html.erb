<!DOCTYPE html> 
<html> 
  <head> 
       <meta charset="utf-8"> 
       <title>UvA courses| <%= yield(:title) %></title>
       <link rel="stylesheet" href="http://code.jquery.com/mobile/1.2.0/jquery.mobile-1.2.0.min.css">
       <script src="http://code.jquery.com/jquery-1.8.2.min.js"></script> 
       <script src="http://code.jquery.com/mobile/1.2.0/jquery.mobile-1.2.0.min.js"></script>
       <meta name="viewport" content="width=device-width, initial-scale=1">
 </head> 
 <body>
 
 <%= javascript_include_tag :all %>
 
 <div data-role="page">
  <div data-role="header">
    <h1>UvA courses</h1>
    <% if signed_in? %>
            <%= link_to "Sign out",  signout_path, :method => :delete, "data-icon" => "star", "class" => "ui-btn-right" %>
            <%= link_to "Profile", current_user, "data-icon" => "home", "class" => "ui-btn-left" %>
    <% else %>
    		 <%= link_to "Home", root_path , "data-icon" => "home", "class" => "ui-btn-left" %>
             <%= link_to "Sign in", signin_path , "data-icon" => "plus", "class" => "ui-btn-right" %>
          <% end %>
  </div>
  
  <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
  </div>

 <%= yield %>
 
     <div data-role="footer" data-position="fixed">
       <div data-role="navbar">
         <ul>
      		<li><%= link_to "About",   about_path %></li>
      		<li><%= link_to "Contact", contact_path %></li>
      		<li><%= link_to "Help", help_path %></li>
    		</ul>
        </div> 
      </div>
  </div>
 </body> 
</html>